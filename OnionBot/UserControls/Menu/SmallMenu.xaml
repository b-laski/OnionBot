<UserControl x:Class="OnionBot.UserControls.Menu.SmallMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OnionBot.UserControls.Menu"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,8,0,0">
                <material:PackIcon Kind="wechat" />
            </Button>
            <Button Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,8,0,0">
                <material:PackIcon Kind="BulletinBoard"/>
            </Button>
            <Button Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,8,0,0">
                <material:PackIcon Kind="MusicBox" />
            </Button>
            <Button Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,8,0,0">
                <material:PackIcon Kind="Settings" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
